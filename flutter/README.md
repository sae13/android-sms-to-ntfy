# Flutter SMS → ntfy

Flutter configuration UI backed by Android Platform Channels. The Android receiver and foreground service continue forwarding while the UI is closed.

## Run

Install Flutter 3.22+, then run `flutter pub get`, `flutter test`, and `flutter run` from this directory. Android 6+ is supported. Grant SMS and notification permissions when prompted.
